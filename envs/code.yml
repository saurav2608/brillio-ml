name: code 
channels:
  - conda-forge
dependencies:
  # required
  - python=3.7.5
  - pandas
  - pip

  - pip:
    - scikit-learn
    - mlflow[extras]
    - azure-storage==0.36.0
    - pytest
    - azure-storage-blob==12.1.0
